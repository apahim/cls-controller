apiVersion: apps/v1
kind: Deployment
metadata:
  name: cls-controller-remote-cluster-management
  namespace: cls-system
  labels:
    app.kubernetes.io/name: cls-controller
    app.kubernetes.io/component: controller
    app.kubernetes.io/instance: remote-cluster-management
    app.kubernetes.io/version: "latest"
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: cls-controller
      app.kubernetes.io/component: controller
      app.kubernetes.io/instance: remote-cluster-management
  template:
    metadata:
      labels:
        app.kubernetes.io/name: cls-controller
        app.kubernetes.io/component: controller
        app.kubernetes.io/instance: remote-cluster-management
    spec:
      serviceAccountName: cls-controller
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
        runAsGroup: 65534
        fsGroup: 65534
      containers:
      - name: controller
        image: gcr.io/apahim-dev-1/cls-controller:20251014223116
        imagePullPolicy: Always
        args:
        - --config=/etc/cls-controller/config.yaml
        - --controller-name=remote-cluster-management
        ports:
        - name: metrics
          containerPort: 8080
          protocol: TCP
        - name: health
          containerPort: 8081
          protocol: TCP
        env:
        # Google Cloud Project
        - name: PROJECT_ID
          value: "apahim-dev-1"
        # CLS-Backend API (internal Kubernetes service)
        - name: CLS_CONTROLLER_API_BASE_URL
          value: "http://cls-backend.cls-system:80/api/v1"
        - name: CLS_CONTROLLER_ORGANIZATION_DOMAIN
          value: "redhat.com"
        - name: CLS_CONTROLLER_AUTO_MAP_ORGANIZATION
          value: "true"
        # Controller identification
        - name: CLS_CONTROLLER_CONTROLLER_NAME
          value: "remote-cluster-management"
        # Pub/Sub configuration (events only - status via API)
        - name: CLS_CONTROLLER_EVENTS_SUBSCRIPTION
          value: "cls-controller-cluster-events"
        # Controller config
        - name: CLS_CONTROLLER_CONFIG_NAMESPACE
          value: "cls-system"
        - name: CLS_CONTROLLER_CONFIG_NAME
          value: "remote-cluster-management"
        # Authentication
        - name: GOOGLE_APPLICATION_CREDENTIALS
          value: "/etc/gcp/key.json"
        volumeMounts:
        - name: config
          mountPath: /etc/cls-controller
          readOnly: true
        - name: gcp-key
          mountPath: /etc/gcp
          readOnly: true
        resources:
          limits:
            cpu: 500m
            memory: 512Mi
          requests:
            cpu: 100m
            memory: 128Mi
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
      volumes:
      - name: config
        configMap:
          name: cls-controller-config
      - name: gcp-key
        secret:
          secretName: cls-backend-gcp-key
          items:
          - key: key.json
            path: key.json
      terminationGracePeriodSeconds: 10